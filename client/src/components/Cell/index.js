/**
 * External Dependencies
 */
import styled, { css } from 'styled-components';

const cellStyles = css`
    display: table-cell;
    vertical-align: middle;
`;

export default styled.div`${ cellStyles }`;
