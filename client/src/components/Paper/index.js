/**
 * External Dependencies
 */
import styled, { css } from 'styled-components';

const paperStyles = css`
	background-color: #fff;
	box-shadow: 0 4px 0 #eee;
	margin-bottom: 4px;
	overflow: hidden;
	position: relative;
`;

export default styled.div`${ paperStyles }`;
