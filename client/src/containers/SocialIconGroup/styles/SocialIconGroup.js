/**
 * External Dependencies
 */
import styled, { css } from 'styled-components';

export const socialIconGroup = css`
    position: absolute;
    right: 0;
    margin: 0;
    top: 0;
    margin-right: 10px;
`;

const SocialIconGroup = styled.ul`${ socialIconGroup }`;

export default SocialIconGroup;
