// Just a test blog for now, until I have more posts to play with.
export const SITE_URL = 'ijustwanttotesttheapi.wordpress.com';

// It seems that the portfolio endpoint isn't available on WP-API v1.
// It also seems that the only way to use v2 is to
// have the plugin installed on a hosted site (i.e. not a wp.com one).
// For now, until there is some other option,
// I'll use another site to host 'projects' (these will just be blog posts).
// Once the site is ported over as a WP theme I'll replace the calls with v2 endpoints.
export const PROJECTS_SITE_URL = 'projectsbyspen.wordpress.com';
